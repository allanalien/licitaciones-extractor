{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python src/main.py --mode=scheduler",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}